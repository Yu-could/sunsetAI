# 夕阳智语 SunsetAI — 优化修改日志

> 最后更新：2026-06-20  
> 项目：`c:\Users\26293\Desktop\创意大赛`  
> 地址：http://localhost:5175/sunsetAI/#/

---

## 2026-06-20 更新 #4 — 头像上传 & 个人主页完善

### 头像更换功能
| 文件 | 改动 |
|------|------|
| `src/views/RealName.vue` | 顶部头像可点击 → 触发文件选择器上传图片 |
| `src/views/RealName.vue` | 移除顶部 × 关闭按钮，保留底部返回按钮 |
| `src/components/UserAvatar.vue` | 新增自定义图片显示：**图片 > 姓名首字 > 通用图标** |
| `src/stores/appStore.js` | `saveRealNameInfo` 新增 `avatar` 字段持久化 |

### 头像优先级
1. 自定义上传图片（base64 存储）
2. 姓名最后一个字 + 渐变背景
3. 通用 UserIcon 人形图标

---

## 2026-06-20 更新 #3 — 个人主页改造

### RealName.vue 升级为完整个人主页

| 区块 | 字段 |
|------|------|
| **顶部卡片** | 头像 + 姓名 + 年龄/性别/血型快捷展示 |
| **基本信息** | 姓名、性别、出生日期（自动计算年龄）、身份证号 |
| **健康信息** | 血型、过敏史、身高、体重、慢性病史、用药备注 |
| **紧急联系人** | 姓名、电话、与本人关系 |
| **居住信息** | 地址、居住状况（独居/与配偶/与子女/养老机构） |

### 头像点击跳转
| 位置 | 跳转目标 |
|------|----------|
| 移动端 Home.vue 头部头像 | → `/realname` |
| 桌面端 Home.vue 头部头像 | → `/realname` |
| 桌面端 Layout.vue 侧边栏用户区 | → `/realname` |

### 交互优化
- 全部 `alert()` 替换为 Toast
- 性别/血型/过敏史/居住状况 使用选择器
- 慢性病史多选标签

---

## 2026-06-20 更新 #2 — 引导页 & 子女端修复

### 引导页优化
| 文件 | 改动 |
|------|------|
| `src/components/Onboarding.vue` | 3 步主图标（🌅🎤👨‍👩‍👧）→ 应用 Logo 图片 |
| `src/components/Onboarding.vue` | 内容区 emoji → AppIcon 组件 |
| `src/components/Onboarding.vue` | 序号 emoji → 蓝色数字圆形标记 |

### Bug 修复
| 问题 | 原因 | 修复 |
|------|------|------|
| 子女端导航显示父母菜单 | `window.location.pathname` 无法识别 hash 路由 | 改为 `route.path.startsWith('/child')` |

---

## 2026-06-20 更新 #1 — 全面 UI 优化

### 一、配色体系 🎨

| 变量 | 旧值 | 新值 |
|------|------|------|
| parent（父母端） | `#d4a574` | `#E8843C` |
| parent-hover | `#c49564` | `#D4732E` |
| bg（页面背景） | `#f8f6f3` | `#FFF9F5` |
| child（子女端） | `#74a5d4` | `#5B9BD5` |
| text | `#1a1a1a` | `#2D2D2D` |

新增语义色：`success` / `warning` / `danger` / `info`

**涉及文件**：`tailwind.config.js`、`src/style.css`

### 二、图标升级 🏷️

**新建 `src/components/AppIcon.vue`** — 60+ Heroicons SVG 映射

所有页面标题 emoji → AppIcon 组件：

| 页面 | 旧 | 新 |
|------|-----|-----|
| Entry.vue | 👴 👶 📱 🖥️ | UserIcon, PhoneIcon, GlobeIcon |
| Home.vue | 👵 🌿 🎤 | UserIcon, SparklesIcon, MicrophoneIcon |
| Health.vue | ❤️ | HeartIcon |
| Medicine.vue | 💊 | DeviceTabletIcon |
| Memories.vue | 📖 | BookOpenIcon |
| Diet.vue | 🍜 | FireIcon |
| Safety.vue | ✅ | CheckCircleIcon |
| Family.vue | 👨‍👩‍👧 | UsersIcon |
| Schedule.vue | 📅 | CalendarDaysIcon |
| Settings.vue | ⚙️ | CogIcon |
| SOS.vue | 🚨 | ExclamationTriangleIcon |

### 三、新增组件 🧩

| 组件 | 文件 | 说明 |
|------|------|------|
| Toast | `src/components/Toast.vue` + `src/stores/toastStore.js` | 全局消息提示，替代 alert() |
| FabSOS | `src/components/FabSOS.vue` | 右下角红色脉动 SOS 按钮 |
| UserAvatar | `src/components/UserAvatar.vue` | 智能头像（图片 > 姓名 > 图标） |
| Onboarding | `src/components/Onboarding.vue` | 首次 3 步引导页 |
| SkeletonCard | `src/components/SkeletonCard.vue` | 骨架屏占位卡片 |

### 四、布局优化 📐

| 页面 | 改动 |
|------|------|
| Home.vue（移动） | 功能卡片 3×2 网格，SOS 独立红色横幅，用药数据从 Store 读取 |
| App.vue | 底部导航：SOS → 家人，毛玻璃效果，页面过渡动画 |
| desktop/Layout.vue | 侧边栏导航分组，激活状态指示器，Logo 图标 |

### 五、文件变更汇总

| 类型 | 数量 | 文件 |
|------|------|------|
| 新建 | 8 | AppIcon.vue, UserAvatar.vue, Toast.vue, FabSOS.vue, Onboarding.vue, SkeletonCard.vue, toastStore.js, 修改文档.md |
| 修改 | 18 | tailwind.config.js, style.css, App.vue, appStore.js, Entry.vue, Home.vue, Health.vue, Medicine.vue, Memories.vue, Diet.vue, Safety.vue, Family.vue, Schedule.vue, Settings.vue, SOS.vue, RealName.vue, desktop/Home.vue, desktop/Layout.vue |

---

## 验证步骤

```bash
cd "c:\Users\26293\Desktop\创意大赛"
npm run dev
# 桌面端：http://localhost:5175/sunsetAI/#/
# 手机端：http://192.168.31.55:5175/sunsetAI/#/
```

| # | 验证项 |
|---|--------|
| 1 | 配色：暖奶油背景 + 夕阳橙头部 |
| 2 | Toast：签到/用药/健康录入弹出动画提示 |
| 3 | SOS 按钮：父母端子页右下角红色脉动按钮 |
| 4 | 图标：所有标题使用统一 Heroicons SVG |
| 5 | 头像：点击进入个人主页 → 可上传照片 |
| 6 | 引导页：清除 `sunsetai_onboarding_done` 后刷新 |
| 7 | 子女端：`/child` 底部导航正确显示 |
| 8 | 个人主页：完整信息表单（健康/联系人/居住） |
| 9 | 构建：`npm run build` 无报错 |
